name: Build & Push 88frp-node Docker Image

on:
  push:
    branches: [main, master]
    paths:
      - '88frp-node/**'
  workflow_dispatch:  # 允许手动触发

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 代码
        uses: actions/checkout@v4

      - name: 登录 Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 构建并推送 Docker 镜像
        uses: docker/build-push-action@v6
        with:
          context: ./88frp-node
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/88frp-node:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/88frp-node:${{ github.sha }}
